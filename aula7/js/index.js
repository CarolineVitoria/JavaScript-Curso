/*
constantes também são espaços alocados na memória que recebem um valor, mas ao contrário das variáveis os dados atribuídos não podem ser alterados.
também não podem ser declaradas e só depois inicializá-las com um valor como nas variáveis.
*/
const primeiroNumero = 3;
const segundoNumero = 10;
const calculo = primeiroNumero*segundoNumero;
console.log(calculo);